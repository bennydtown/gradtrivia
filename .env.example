# Required in production — no defaults, app will refuse to start without these
ADMIN_PASSWORD=
SESSION_SECRET=

# Optional — used to build absolute URLs for QR codes; falls back to the request host
PUBLIC_BASE_URL=
